# PyPath — Python for AI
ข้อกำหนดเว็บแอปบทเรียนภาษาไทย v0.1 — ขอบเขตที่ใช้พัฒนาหลังผู้ใช้ตอบตกลง

## เป้าหมายและผู้เรียน
ผู้เริ่มต้นสามารถเรียน Python สำหรับ AI ตามลำดับ อ่านตัวอย่าง ทำแบบฝึกหัด ตรวจความเข้าใจ และกลับมาเรียนต่อได้ด้วย Next.js web app

## สิ่งที่ผู้ใช้ระบุ
- นำคอร์ส Python สำหรับ AI ที่คุยไว้มาเป็นเว็บแอป
- ใช้ Next.js
- เนื้อหาภาษาไทยตามบทสนทนา

## ขอบเขตที่เสนอสำหรับรุ่นแรก
รายละเอียดส่วนนี้เสนอใน PRD และใช้เป็นขอบเขตรุ่นแรกหลังผู้ใช้ตอบ “ok”
- 8 สัปดาห์ สัปดาห์ละ 1 บทหลัก พร้อมแนวคิด โค้ด ผลลัพธ์ตัวอย่าง แบบฝึกหัด และแบบทดสอบ
- หน้าหลักแสดงหลักสูตร ค้นหา กรองสถานะ และความคืบหน้า
- หน้าอ่านบทเรียน เข้าถึงผ่านลิงก์ของแต่ละบท ย้อนกลับและไปบทถัดไปได้
- ตอบแบบทดสอบถูกแล้วจึงกดบันทึกว่าเรียนจบได้ แบบฝึกหัดเขียนโค้ดให้ผู้เรียนทำเอง
- บันทึกบทที่เรียนจบใน localStorage ของเบราว์เซอร์เดิม และแจ้งหากบันทึกไม่ได้
- ใช้ได้บนมือถือและเดสก์ท็อป รองรับการใช้งานด้วยคีย์บอร์ด
- แสดงโค้ดและคัดลอกไปทดลองใน Python 3 / notebook ภายนอก

## โครงสร้างเนื้อหา
1. ตัวแปร ชนิดข้อมูล และเงื่อนไข
2. List, Dictionary, Function และการจัดระเบียบโค้ด
3. เตรียมข้อมูลด้วย pandas: ค่าว่างและข้อมูลซ้ำ
4. สถิติพื้นฐาน เวกเตอร์ และการสำรวจข้อมูล
5. Feature, Label และโมเดล Regression แรก
6. ชุดทดสอบ Baseline, MAE, Precision/Recall และ Data Leakage
7. แนวคิด Neural Network / LLM และตรวจผลลัพธ์ JSON ที่จำลองจาก API
8. โปรเจกต์จำแนกข้อความ: baseline ตามกฎ แล้วต่อยอดเป็นโมเดล

ระยะ 8 สัปดาห์รวมเวลาฝึกเองและทำโปรเจกต์ ไม่ใช่ปริมาณบทอ่านเต็ม 40 วัน ตัวเลขนาทีของแต่ละบทเป็นเวลาโดยประมาณสำหรับกิจกรรมหลัก เนื้อหาปัจจุบันเป็นบทเริ่มต้น ต้องขยายเพิ่มเติมหากต้องการหลักสูตรรายวันเต็มรูปแบบ

## เส้นทางใช้งาน
เปิดหลักสูตร → เลือกบทหรือเรียนต่อ → อ่านแนวคิดและโค้ด → ทดลองภายนอก → ทำแบบฝึกหัด → ตอบแบบทดสอบพร้อมเหตุผล → บันทึกเรียนจบ → ไปบทถัดไป

ผู้เรียนข้ามไปทบทวนบทใดก็ได้ การเรียนจบสะท้อนการผ่านแบบทดสอบความเข้าใจ ไม่ใช่การตรวจโค้ดหรือรับรองทักษะ

## เกณฑ์รับงาน
- [x] ติดตั้ง dependency และ production build สำเร็จ รวมถึง TypeScript check
- [x] เปิดหน้าหลักและทั้ง 8 บทได้ ไม่มีข้อผิดพลาดขณะใช้งาน
- [x] ค้นหาและตัวกรองให้ผลถูกต้อง รวมกรณีไม่พบรายการ
- [x] ตรวจคำตอบผิด/ถูก แสดงเหตุผล และลองใหม่ได้
- [x] บันทึกบทเรียนซ้ำไม่เพิ่มจำนวน และ refresh แล้วยังเก็บความคืบหน้าได้
- [x] ค่า localStorage ที่เสียหรืออ่านไม่ได้ไม่ทำให้เว็บล่ม
- [x] ลิงก์บทเรียนและปุ่มย้อนกลับของเบราว์เซอร์เปลี่ยนบทถูกต้อง
- [x] ตรวจหน้าจอมือถือและเดสก์ท็อป รวมเมนูและโค้ดที่ยาว
- [x] คัดลอกโค้ดใช้งานได้ และข้อความบนหน้าจอไม่อ้างว่ารันโค้ดจริง

## โครงสร้างทางเทคนิคที่เสนอ
- Next.js App Router + TypeScript + React พร้อม CSS สำหรับหน้าจอหลายขนาด
- ข้อมูลหลักสูตรแยกใน app/course.ts เพื่อให้หน้า overview และบทเรียนใช้เนื้อหาเดียวกัน
- หน้าการเรียนและสถานะอยู่ฝั่ง client โดยใช้ localStorage; ยังไม่มี backend contract
- ใช้ URL hash สำหรับเลือกบทในรุ่นแรก โดยเก็บเส้นทาง / ไว้เป็นหน้าหลัก
- ยังไม่เพิ่ม database หรือระบบ plugin เพราะไม่มีข้อมูลผู้ใช้ฝั่งเซิร์ฟเวอร์ที่ต้องจัดเก็บ

## นอกขอบเขตรุ่นแรกที่เสนอ
ระบบบัญชีและซิงก์หลายเครื่อง, รัน Python ในเว็บ, AI tutor/API จริง, ตรวจโค้ดอัตโนมัติ, ระบบชำระเงิน และการเผยแพร่ขึ้นโฮสต์

## ผลตรวจรับ
- `npm run build`: ผ่าน — Next.js สร้างหน้า production สำเร็จ
- `npm run typecheck`: ผ่าน — เปิด strict TypeScript
- `npm test`: ผ่าน 8 กรณีใน Chrome (12.7 วินาที) ครอบคลุมทุกบท การค้นหา/กรอง การตอบผิดและลองใหม่ ความคืบหน้าครบคอร์ส ข้อมูลในเครื่องที่เสียหรือบันทึกไม่ได้ clipboard และคีย์บอร์ด
- ตรวจภาพหน้าจอจริงที่ 1440px และ 390px รวมหน้าบทเรียนมือถือ และไม่พบเนื้อหาล้นความกว้างหน้าจอ
- โค้ดตัวอย่าง Python แสดงพร้อมผลลัพธ์ประกอบ การทดสอบข้างต้นตรวจเว็บ ไม่ได้รัน Python ทุกตัวอย่าง

## เปิดใช้งานในเครื่อง
โปรเจกต์: `/Users/kristalyn/Documents/GitHub/python-ai-course`

ใช้ `npm run dev` สำหรับพัฒนา หรือ `npm run build` แล้ว `npm run start` เพื่อเปิด production ที่ http://localhost:3000

`npm test` ใช้ Google Chrome ที่ติดตั้งในเครื่อง และต้องมี production build ก่อนทดสอบ

## รุ่น 0.2 — เส้นทางต่อเนื่อง 5 คอร์ส
ผู้ใช้ขอเพิ่มคอร์ส 2–5 เพื่อพัฒนาความเชี่ยวชาญด้าน AI
- คอร์ส 2: Machine Learning เชิงลึก — คณิตศาสตร์, pipeline, validation, metrics, tuning และโปรเจกต์
- คอร์ส 3: Deep Learning — tensor, gradient, training loop, regularization, CNN, attention และโปรเจกต์
- คอร์ส 4: LLM & RAG — token, retrieval, context, evaluation, tool use, fine-tuning และโปรเจกต์
- คอร์ส 5: AI Engineering — contract, serving, monitoring, experiment tracking, safety, research และโปรเจกต์
- คอร์สละ 8 บท รวม 40 บท มีตัวอย่างโค้ด แบบฝึกหัด และคำถามพร้อมเหตุผลทุกบท
- เลือกคอร์สผ่านหน้าหลักได้ แสดงพื้นฐานที่ควรมีก่อนเรียนและแหล่งอ่านต่อ
- แยกความคืบหน้าต่อคอร์ส โดยคอร์สแรกยังใช้ storage key และลิงก์เดิม
- URL ของคอร์สใหม่: #course-2 และ #course-2/lesson-1 เป็นต้น
- ตัวอย่างขั้นสูงอาจเป็นการจำลองแนวคิด ระบุว่าใช้ข้อมูลจำลองและแยกจากงานโปรเจกต์จริง
- ไม่รับรองว่าเรียนจบแล้วเป็นผู้เชี่ยวชาญ ต้องมีผลงานและผลการทดลองที่ทำซ้ำได้

### ลำดับดำเนินงาน
- [x] เพิ่มข้อมูลคอร์สและบทเรียน 32 บท
- [x] เพิ่มการเลือกคอร์สและแยกความคืบหน้า
- [x] ตรวจเนื้อหา build และทดสอบทุกคอร์ส รวมการกลับมาใช้ข้อมูลเดิม


### ผลตรวจรุ่น 0.2
- Production build และ TypeScript ผ่าน
- Browser tests ผ่าน 10 กรณี รวมเปิดบทใหม่ครบ 32 บท สลับคอร์ส เก็บความคืบหน้าแยกจากคอร์สเดิม และประวัติลิงก์บนมือถือ
- ตรวจภาพหน้าเลือกคอร์สและคอร์ส 5 บนมือถือ ไม่พบความกว้างล้นหน้าจอ
- `python3 scripts/check-examples.py`: ตรวจ syntax โค้ดใหม่ครบ 32 ตัวอย่าง และรัน 20 ตัวอย่างที่ใช้ standard library ได้ผลตรงกับเนื้อหา
- อีก 12 ตัวอย่างที่ใช้ PyTorch/scikit-learn ตรวจ syntax แล้ว แต่ยังไม่ได้รันใน Python environment นี้
- เพิ่มแนวทางเลือกสาย Computer Vision, NLP, AI Research และ Reinforcement Learning หลังคอร์ส 5 เป็น roadmap ไม่ใช่บทเรียนเพิ่มเติมที่เปิดแล้ว

## รุ่น 0.3 — เรียนตามหลักฐานจากแบบทดสอบ
- แบบประเมินใหม่ 3 ระดับต่อคอร์ส ระดับละ 4 ข้อ รวม 60 ข้อ แยกจาก quick quiz เดิม
- ใช้คำตอบครั้งแรกต่อข้อเท่านั้นในการแนะนำระดับ ต้องตอบครบ 4 ข้อและถูกอย่างน้อย 3 ข้อเพื่อผ่านระดับนั้น
- ผ่านระดับ 1 แนะนำบท 4; ผ่านระดับ 2 แนะนำบท 7; ผ่านระดับ 3 แนะนำโปรเจกต์และการประเมินคอร์สถัดไป
- ถ้าผิด ชี้บททบทวนตามทักษะของข้อที่ผิด แสดงหลักฐานรายข้อและจำนวนข้อมูล ไม่สรุปเป็น IQ หรือความเชี่ยวชาญ
- เก็บผล quick quiz ครั้งแรกแยกจากผล placement ไม่ใช้คะแนนง่ายแทนการผ่านข้อยาก
- ฝึกซ้ำได้แต่ไม่เพิ่มคะแนนประเมิน; ไม่มีการล็อกบทเรียน ผู้ใช้เปิดโจทย์ยากเองได้แม้ผลยังไม่ถึงเกณฑ์
- ไม่มีการทำเครื่องหมายบทที่ข้ามว่าเรียนจบอัตโนมัติ และข้อมูลความคืบหน้าเดิมยังอยู่
- บันทึกผลในเบราว์เซอร์พร้อม version ของคลังข้อสอบ; รับมือข้อมูลเสียและการบันทึกไม่ได้
- ผลเป็นคำแนะนำจากคลังข้อจำกัด ไม่ใช่แบบทดสอบมาตรฐานหรือการตรวจทักษะเขียนโค้ดจริง

### ขั้นตอนรุ่น 0.3
- [x] สร้างคลังคำถามและกติกาวิเคราะห์แยกจาก UI
- [x] เพิ่มหน้าแบบประเมิน หลักฐาน และลิงก์ข้าม/ทบทวน
- [x] เชื่อม quick quiz โดยแยกคะแนนจาก placement
- [x] ตรวจเกณฑ์ขอบเขต การตอบซ้ำ persistence และ UI จริง


### ผลตรวจรุ่น 0.3 (2026-09-12)
- Production build และ TypeScript ผ่าน
- `npm test`: 15 tests passed (15.2s) รวม unit tests ของเกณฑ์ทั้ง 5 คอร์สและ browser workflows
- ตรวจคะแนน 3/4 ต่อเนื่องครบทุกระดับแล้วแนะนำ capstone ถูกต้อง; ระดับสูงผ่านไม่ชดเชยระดับก่อนหน้าที่ไม่ผ่าน
- คำตอบซ้ำหลังเห็นเฉลยไม่เปลี่ยนคะแนนครั้งแรก ผลเก็บหลังรีเฟรชและแยกแต่ละคอร์ส
- Quick quiz แยกหลักฐานจาก placement; บทที่ข้ามไม่ถูกนับเรียนจบ
- ตรวจภาพแบบประเมินบนมือถือ ไม่มีเนื้อหาล้นความกว้างหน้าจอ
- การจัดระดับนี้เป็นกฎของผลิตภัณฑ์ ไม่ใช่แบบทดสอบที่สอบเทียบทางจิตมิติ และไม่วัดความสามารถเขียนโค้ดโดยตรง
